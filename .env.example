# Local development
SOFFICE_PATH=soffice
# Set a strong secret for the score management backend.
ADMIN_KEY=replace-with-a-long-random-secret
# Optional: if set, records are stored in PostgreSQL; otherwise SQLite is used locally.
# DATABASE_URL=postgresql://user:password@localhost:5432/biochemical_training
