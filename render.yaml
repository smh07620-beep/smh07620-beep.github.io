services:
  - type: web
    name: biochemical-training-exam
    runtime: docker
    plan: free
    region: singapore
    numInstances: 1
    healthCheckPath: /health
    autoDeployTrigger: commit
    envVars:
      - key: DATABASE_URL
        sync: false  # 部署時手動在 Render 後台貼入 Supabase 或 Neon 的連線字串
      - key: ADMIN_KEY
        sync: false
      - key: MATERIAL_STORAGE
        value: /var/data/materials
