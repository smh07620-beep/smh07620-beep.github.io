services:
  - type: web
    name: biochemical-training-exam
    runtime: docker
    plan: starter
    disk:
      name: materials-data
      mountPath: /var/data
      sizeGB: 10
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: biochemical-training-db
          property: connectionString
      - key: ADMIN_KEY
        sync: false
      - key: MATERIAL_STORAGE
        value: /var/data/materials

databases:
  - name: biochemical-training-db
    databaseName: biochemical_training
    plan: starter
    disk:
      name: materials-data
      mountPath: /var/data
      sizeGB: 10
    region: singapore
